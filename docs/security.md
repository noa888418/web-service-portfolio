# セキュリティ要件と検証方法のたたき台

## 状態と適用範囲

要件・設計段階からセキュリティを考慮するシフトレフト方針です。以下の「決定済み」は採用する方針を意味し、対策の実装完了を意味しません。秘密情報検査の実装・検証状況は次節のとおりです。それ以外の検証は未実施です。

題材と React + TypeScript + Vite、Laravel、PostgreSQL、Docker、ECS、Terraform、GitHub Actions の使用、AWS 月額予算3,000円、事前案内期間のみの公開はユーザー決定です。月60時間は構築・検証6時間、公開48時間、閉鎖・保存・撤去等6時間を目安とし、初月は公開を短縮できます。独自ドメインは未所有です。業務詳細は [要件案](requirements.md)、AWS配置は [基本設計案](architecture.md)のレビュー対象です。各製品の対応版、追加検査ツール・停止基準、デモ資格情報の配布経路は未決定です。費用条件は [costs.md](costs.md)を参照します。

今回の前提は架空データ専用、同期間の再デプロイは保持、次回公開は初期状態、snapshotは取得後7日・通常最新1世代（復元検証中の一時併存も各世代の元の期限内）、アプリログ7日・アクセスログ30日です。作成者が終了時の資格情報・セッション失効、期限削除と結果確認を担当します。秘密情報の誤投入は期限を待たず対応します。[DB](database.md)・[API](api.md)・[画面](screens.md)・[横断検証計画](design-review.md)は追加の設計案で、未実装・未検証です。

## 秘密情報検査の実装と検証状況

SEC-05・06・11 のうち Git への混入防止を対象としています。イメージへの混入防止、依存関係検査、イメージ検査、SBOM、Terraform / AWS の実環境での保護は未実装・未検証です。

| 対象 | 実装状況 | 検証状況 |
| --- | --- | --- |
| `.gitignore` | env、Terraform state / plan / 作業ディレクトリ / 変数値、秘密鍵などを除外。ダミー見本と lock ファイルは管理可能 | 初回整備時に 19 件の除外と 8 件の管理可能パスを隔離リポジトリで確認済み。当時の追跡済みファイルは 0 件。今回の確認でも ignore 対象の追跡済みファイルはなし |
| Gitleaks | 8.30.1 を `config/gitleaks.json` で固定し、公式アーカイブの SHA-256 を照合。既定ルールを継承し、全値を redact | Windows x64 で導入・バージョン照合・現行ファイル検査を実施し、検出なし |
| ローカル pre-commit | Git 標準フックを `.git/hooks/pre-commit` に登録済み。Git for Windows の sh から既存 Python を呼び、ステージ差分を検査 | 隔離環境の正常コミット成功、ステージ内だけのダミー文字列によるコミット拒否、HEAD 不変、ログの値の伏字化を確認済み |
| 検査不能時・既存フック | Gitleaks 未導入・バージョン違い・実行エラーを非 0 終了とする。既存フックや `core.hooksPath` があれば自動上書きしない | 未導入時のコミット拒否、既存フック・`core.hooksPath` の保全、同一フック再登録を確認済み。バージョン違いの実動作は未検証 |
| GitHub Actions | push / pull_request で取得済み Git 履歴とチェックアウトしたファイルを検査。`contents: read` のみ、認証情報を checkout 後に保持しない | **初回 push に対する検査成功は利用者確認済み**。実行 URL・対象 SHA・詳細ログは未提示で、こちらでの直接確認は未実施。PR の検査、CI での検出時の失敗、必須チェック設定は別途確認が必要。Linux 導入など各ステップのログを個別確認したとは扱わない |

初回整備時の確認環境は Windows PowerShell 5.1、Git 2.48.1.windows.1、Python 3.13.2 です。pre-commit パッケージは未導入のまま、Docker 27.5.1 は検査に使用していません。Docker は CLI の存在のみ確認し、デーモンの稼働は未検証です。

初回整備時の検証コマンドは `python scripts/secrets.py files`、`python scripts/secrets.py staged`、`python -I scripts/test_secret_controls.py` です。当時はステージが空だったため、staged の検出なしだけでは検証済みとせず、その時点のファイルを複製した隔離リポジトリで実際のフックを検証しました。一時リポジトリは終了時に削除し、本リポジトリの index・HEAD が変わっていないことも確認しました。当時は本リポジトリにコミットがなく、その履歴検査は未実施でした。その後の初回 push の結果は上表の利用者確認として区別します。

詳細な再現手順と push 後の確認項目は [開発環境](development.md)を参照してください。Gitleaks はパターン検査であり、任意の秘密情報をすべて発見できる保証はありません。Gitignore やローカルフックだけで混入防止が強制されるわけではないため、CI の必須チェック設定も別途必要です。

## 要件・設計で確認すること（提案）

- 資産：利用者データ、認証情報、DB、ソース、イメージ、AWS 権限、Terraform の状態ファイル。
- 境界：ブラウザーとアプリ、アプリと DB、GitHub Actions と AWS、外部公開部分と内部ネットワーク。基本設計の図とDB/APIのデータフローを参照し、実装時に照合する。
- 脅威：不正入力、権限のない閲覧・変更、秘密情報の漏えい、依存関係の脆弱性、過剰な権限、公開設定の誤り、資源の大量消費。
- 各脅威に、対象要件、対策、検証方法、残るリスクを対応付ける。不要な個人情報を収集しない設計を検討する。

## 決定済み方針と検証案

検証方法・タイミングは以下をたたき台に具体化します。

| ID | 決定済みの方針 | 検証方法案・残す証跡 |
| --- | --- | --- |
| SEC-01 | 要件・設計からセキュリティを考慮する | 実装前と設計変更時にデータ・権限・信頼境界・脅威をレビューし、要件と検証ケースの対応を残す |
| SEC-02 | 必要最小限のベースイメージを使う | Dockerfile と最終イメージのパッケージを確認し、実行に必要なものと選定理由を記録する。容量の小ささだけで選ばず、互換性・更新可能性も確認する |
| SEC-03 | 非 root ユーザーで実行する | Dockerfile の USER と ECS 実行設定を確認し、起動したコンテナの実効 UID が 0 でないこと、必要な読み書きと起動が成功することを確認する |
| SEC-04 | マルチステージビルドを採用する | ビルド用と実行用の段階を確認し、最終イメージに不要なビルドツールやキャッシュがないことを調べ、起動確認を行う |
| SEC-05 | 秘密情報を Git・イメージに含めない | Git の変更と履歴、ビルドコンテキスト、イメージのレイヤー・設定を検査する。除外設定だけでなく検出結果を確認し、ログにも実値を残さない |
| SEC-06 | 秘密情報検出を工程に組み込む | ローカルと PR での検査を設計する。検査対象範囲を記録し、隔離した検証用のダミー文字列で検出・失敗終了を確かめる。実際の秘密情報は使わない |
| SEC-07 | 依存関係検査を工程に組み込む | フロント・バック両方の依存関係とロックファイルを検査し、対象・検査日時・検出結果を保存する。定期的な再検査の頻度を決める |
| SEC-08 | イメージ検査を工程に組み込む | ビルドした配布対象イメージを検査し、ダイジェストと OS・言語パッケージの検出結果を記録する。検査対象とデプロイ対象の一致を確認する |
| SEC-09 | SBOM を生成する | 配布対象イメージに対応する SBOM を生成し、アプリと OS の構成要素が対象に含まれるか確認する。形式、対象ダイジェスト、生成日時を記録する |
| SEC-10 | Terraform の権限・ネットワーク・状態保護を検討する | 下記のインフラ検証案を設計に反映し、設定レビュー、IaC 検査、plan と構築後の確認を行う |
| SEC-11 | テスト・検査を無効化して成功扱いにしない | 必須ジョブと失敗伝播をレビューし、失敗を発生させる制御された検証でマージ・デプロイを止められるか確認する。未実行や検査エラーも合格としない |

SBOM は構成要素の一覧であり、生成できたこと自体を脆弱性がない証明とはしません。

マルチステージによるビルド環境と実行環境の分離は [Docker 公式のビルド指針](https://docs.docker.com/build/building/best-practices/)を参照します。ビルド時に秘密情報が必要な場合は、ARG / ENV や COPY に埋め込まず、[Docker の secret mount](https://docs.docker.com/build/building/secrets/)を検討します。

## サービス固有の「データ → 脅威 → 対策 → 検証」（提案）

以下の APP-SEC 要件は**すべて未実装・未検証**です。FR / AC は [要件案](requirements.md) の機能・受け入れ条件を参照します。共通の開発方針 SEC-01～SEC-11 と区別します。

| ID・関連要件 | 守るデータ | 想定する脅威 | 対策案 | 検証方法案 |
| --- | --- | --- | --- | --- |
| APP-SEC-01：FR-01・FR-02、AC-01～AC-03・AC-07 | アカウント、認証状態、依頼・コメント全体 | 未認証・失効後のアクセス、ログイン失敗からのアカウント存在推測 | 保護 API 全体でサーバー側の認証を必須化。Cookie セッションを提案し、ログアウトで失効。認証失敗の応答を共通化し、パスワードはハッシュで保存。試行回数制限・有効期限はAPI設計の提案値をレビューする | 認証情報なし・無効・ログアウト後で一覧、詳細、投稿、割り当て、状態変更、担当候補取得を直接呼び、GET は 401、変更は 401 または CSRF の 419 と DB 不変を確認。不存在アカウントと誤パスワードの応答を比較 |
| APP-SEC-02：FR-04・FR-05・FR-08、AC-06・AC-08 | 自分以外の依頼、コメント、投稿者、件数情報 | 社員 A が B の ID を推測・改ざんして閲覧や投稿する | サーバーが毎回、役割と依頼者を検証。社員の一覧・件数は本人の範囲に限定。コメントは親依頼配下のみ提供し、他人の親URLと不存在を同じ404、閲覧可の親への保護項目追加を403にする | 社員 2 人の別認証状態で URL・本文・一覧条件を差し替える。404、他人の本文・表示名・件数の非露出、コメント件数不変を確認。IT担当者の正当な全件閲覧も確認 |
| APP-SEC-03：FR-01・FR-03・FR-06・FR-07・FR-08、AC-09～AC-11・AC-15 | 依頼者、投稿者、担当者、状態、アカウントの役割 | 社員が担当・状態変更 API を直呼びする、role 等を追加して昇格・なりすましする | 操作ごとに役割をサーバーで確認し、許可された入力だけを受け付ける。依頼者・投稿者は認証中の本人から設定。保護項目の追加は 403。Web の役割変更手段は設けない。IT担当者でも任意ユーザー更新は不可 | 社員が自分の依頼に対して担当・状態変更を試し 403 を確認。ログイン・登録・コメントへ role / 他人の ID 等を追加しても成功せず、DB の役割・所有者・担当・状態が不変であることを確認 |
| APP-SEC-04：FR-06～FR-08、AC-10～AC-12・AC-14・AC-15 | 担当、状態、完了後の依頼・コメントの整合性 | 未割当で対応開始、状態飛び越し、完了後投稿、同時更新で条件回避 | 保存直前にも遷移・状態・担当者を検証し、競合を検知。完了後の更新と提供しない編集・削除を拒否。必要な原子的更新を設計する | 表の全許可・禁止遷移、社員を担当に指定、完了後操作を API で検証。同時要求で 409 と整合した保存結果を確認 |
| APP-SEC-05：FR-03・FR-05・FR-08、AC-05・AC-13 | 依頼・コメントと、それを読む社員・IT担当者の認証状態 | 保存型 XSS、不正な SQL 入力、巨大な本文の投入 | サーバー側で型・必須・長さ・列挙値を検証。プレーンテキスト表示時にエスケープし、安全なパラメーター化クエリを使う。リクエスト全体の上限も設計時に定める | タグや SQL 構文を模した入力で、スクリプト非実行・意図しないデータ参照や更新なしを確認。日本語を含む上限・上限超過、型違いで期待する拒否結果を確認 |
| APP-SEC-06：FR-01～FR-08、AC-18・AC-19 | 認証情報、本人の操作意思、依頼内容の通信 | 認証情報の盗聴、CSRF、キャッシュによる他人の応答・Cookie の漏えい | ブラウザーからは HTTPS、host-only の Secure / HttpOnly / SameSite Cookie、CSRF ヘッダー検証。API・認証は CloudFront CachingDisabled と private, no-store、必要な Cookie・header を転送。内部 HTTP の範囲は基本設計に明示 | Cookie 属性、CSRF 値なし・不正値の 419 と DB 不変、ログアウト後の非表示を確認。A・B の連続アクセスで cache Hit や応答・Set-Cookie の混在がないことを確認 |
| APP-SEC-07：FR-09、AC-16・AC-17 | 初期アカウント、初期資格情報、架空データ | 固定パスワードの公開、実業務環境へのデモアカウント混入、再実行での上書き | 実行設定（APP_ENV=production、APP_DEBUG=false）と demo 用途を分離。専用環境・許可 DB・明示許可を満たす場合だけ別ジョブで架空データ投入。秘密値をソース外から供給し、通常デプロイで seed や migrate:fresh を実行しない | production 設定の許可済み demo で成功し、非 demo・対象違い・許可なしで APP_ENV に関係なく拒否することを確認。再実行で既存データ・資格情報が不変、Git・イメージ・ログに平文秘密値がないことも確認 |
| APP-SEC-08：FR-03・FR-08・FR-09、保存・削除方針 | メール、依頼・コメント本文、認証情報、ログ・バックアップ | 実在の機密情報の誤投入、ログ漏えい、保存期限後の残留、退職後アクセス | 架空データ利用と秘密情報を入力しない案内。ログは必要最小限の識別情報と操作結果に限定し、認証情報・本文全文を記録しない。指定済み保存期間・作成者責任に従い、DB管理手順で削除・資格情報失効を行う | 応答・通常ログ・例外ログに秘密値がないか確認。実装後、運用削除とバックアップへの反映、資格情報失効後のアクセス拒否、復元手順を検証 |

認可はボタンの非表示だけで完了としません。未認証、社員 A・B、IT担当者 X・Y を使い、役割・依頼者・状態を変えた直接要求で正常系と拒否系を対にして検証します。認可の試験では適切な CSRF 条件を整え、CSRF 自体の拒否試験と分けます。拒否時は HTTP 応答だけでなく DB の不変と情報の非露出を確認します。試験結果は実施後に別途記録し、この表を検証成功の証跡として扱いません。

今回の具体化：APP-SEC-01は無操作30分/絶対8時間、停止フラグ・auth_version、session直列化、メール5回/分・IP30回/分の429を提案。APP-SEC-02/03は親URLの不可・不存在を404、閲覧可の親への保護項目追加は403とします。APP-SEC-04は担当の複合FK・担当必須CHECK、整数version、users→親依頼のロック順で具体化しました。APP-SEC-07/08は投入記録によるno-opと今回の保存・失効前提に従います。試行制限は共有デモアカウントへの一時的な利用妨害も起こり得るため、数値・配布方法とログ監視を公開前に確認します。

対策の考え方は [OWASP の認可ガイド](https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html)の要求ごとの権限確認と拒否を既定にする方針、[一括代入の対策ガイド](https://cheatsheetseries.owasp.org/cheatsheets/Mass_Assignment_Cheat_Sheet.html)の入力許可リストを参考にしています。具体的な役割・遷移・エラー契約は本サービスの提案です。

## Terraform・AWS・CI/CD の対策案（未実装・未検証）

[architecture.md](architecture.md)でネットワーク・暗号化区間・Provider v6.65.0 の引数・依存順を整理しました。VPC origin → 非公開 ALB → ECS の受信元を SG で絞り、ECS の public IPv4 は外向き通信に用います。RDS は非公開です。ブラウザー → CloudFront は HTTPS、CloudFront → ALB → Nginx は内部 HTTP であり、全区間暗号化済みではありません。内部HTTPと標準証明書のTLS制約は**架空データ専用デモの例外**です。理由はドメイン未所有・小予算・短期公開、残存リスクは内部平文Cookie/本文と最低TLS制限の弱さです。実在データ、常設化・利用者拡大、厳格なTLS要件、独自ドメイン取得・仕様変更時には見直します。

| 対象 | 設計案 | 検証案 |
| --- | --- | --- |
| IAM | Terraform 実行・デプロイ・アプリ実行の役割と必要最小限の権限を整理する | ポリシー、信頼条件、対象リソースをレビューし、許可した操作と拒否する操作を確認 |
| CI の AWS 認証 | GitHub Actions の OIDC による短期認証を基本設計の方針案とし、aud と sub、対象リポジトリ・Environment の条件を限定。PR と apply / deploy の権限を分ける | 信頼ポリシーと環境設定を確認し、許可された実行条件でのみ認証できることを確認 |
| ネットワーク | 公開入口を限定し、DB への接続元をアプリ等の必要な範囲に絞る | Terraform plan と構築後の設定を照合し、許可経路の疎通と許可外経路からの拒否を確認 |
| 状態・plan | S3 backend の暗号化・versioning・use_lockfile を提案。bootstrap / foundation / edge / runtime で state と権限を分け、通常撤去から state・保存データを除外。Git・公開 artifact に含めない | バックエンド設定、権限、公開不可、同時実行時のロック、復元と runtime 撤去後の保存対象残存を確認。state / plan の実値を報告へ貼らない |
| 実行時の秘密情報 | 秘密情報ストアから注入する方法、参照権限、更新・失効手順を決める | 権限と注入後の起動、ログへの非露出、認証情報の更新手順を確認 |
| ワークフロー | 必要最小限の権限を設定し、信頼できない PR に秘密情報・デプロイ権限を渡さない。外部 Action の参照固定と更新方法を検討する | イベント条件、権限、外部参照、必須チェックをレビューし、失敗した成果物が配布されないことを確認 |

Terraform の `sensitive` は表示を抑制する仕組みで、状態ファイルに秘密情報が保存されない保証ではありません。[HashiCorp の機密データ管理](https://developer.hashicorp.com/terraform/language/manage-sensitive-data)を踏まえ、状態ファイル自体を保護します。[GitHub の AWS OIDC ガイド](https://docs.github.com/en/actions/how-tos/secure-your-work/security-harden-deployments/oidc-in-aws)を参照し、長期 AWS キーを保管しない方針案にしています。詳細な権限・復旧操作は実装前にレビューします。

公開・撤去では state bucket や snapshot を一括 destroy に含めず、DB の復元確認と旧世代の削除を分離します。保存中は復号鍵を維持します。予算通知は強制停止ではないため、公開終了とリソース残存の確認を運用者が行う案です。依存関係・イメージ・SBOM・Terraform の追加検査案は基本設計に記載していますが、Gitleaks 以外の導入・検証を完了したとは扱いません。

削除確認は作成者が行い、復元検証中の旧snapshotも7日期限を延長しません。誤投入では閉鎖・該当資格情報失効・影響調査・汚染したDB行/snapshot/ログの除去を先行させ、通常の保持期限を待ちません。削除操作の証跡に秘密値を転記せず、再復元による再混入を防ぎます。

## 工程への組み込み案

1. **要件・設計**：データ、脅威、権限、受け入れ条件、検査の合否基準を整理する。
2. **実装・PR**：関連する自動テスト、秘密情報・依存関係・IaC の検査、設計レビューを実施する。
3. **イメージ作成・公開前**：最終イメージの非 root 起動、イメージ検査、SBOM 生成と対象の一致を確認する。必須チェック通過を公開条件にする。
4. **公開後・依存関係更新時**：脆弱性の再検査、権限・ログの確認、更新・復旧の検証を行う。頻度は未決定。

## 合否・証跡・検出時の対応

- **決定済み**：失敗・未実施を成功扱いにしない。テストや検査を無効化して通さない。
- **秘密情報検査で決定済み**：Gitleaks 8.30.1 の既定ルールを使用し、1 件以上の検出で失敗する。検査エラーも失敗とし、全秘密値を伏せる。インラインの `gitleaks:allow` による除外は許可しない。現在は独自 allowlist・baseline を設定していない。
- **未決定・要確認**：秘密情報検査以外の検査ツール、脆弱性の重大度・修正可能性に応じた停止基準、追加の必須チェック、定期実行頻度、証跡の保存先・期間。GitHub の `Gitleaks` 必須チェック設定は別途確認が必要で、設定済みとして扱わない。検査導入時に基準を明文化し、基準未決定の状態を公開可能とみなさない。
- **提案**：検出内容を調査し、修正して同じ検査を再実行する。誤検知なら根拠・対象・確認者・期限を記録し、除外はその対象に限定する。検査全体の停止や、都合のよい基準変更で解消扱いにしない。
- **提案**：検証結果には要件 ID、対象コミットまたはイメージダイジェスト、実行環境、ツール・ルールのバージョン、日時、結果、対応内容を記録する。証跡に秘密情報を含めない。
- **提案**：秘密情報の漏えいが判明した場合は、対象を失効・再発行し、影響範囲とログ・履歴・イメージへの混入を調査する。ファイルを削除しただけで解決扱いにしない。

この文書は検証計画です。テストやスキャンで確認できる範囲と、レビューや実環境での確認が必要な範囲を区別し、完全な安全性を保証するものとして扱いません。
