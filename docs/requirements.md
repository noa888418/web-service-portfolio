# 要件整理のたたき台

## 状態と目的

サービスの題材・初期機能は未決定です。この文書は要件を決めるための枠組みであり、以下の提案や記入欄は確定仕様ではありません。アプリの実装・検証は未実施です。開発環境の秘密情報検査は実装・ローカル検証済みで、GitHub Actions 上での実行確認は未実施です。

目的は、小規模な Web サービスを完成させ、利用者の課題から設計・実装・検証までのつながりを面接で説明できるようにすることです。共通の決定事項は [README](../README.md)、セキュリティの詳細は [security.md](security.md) を参照します。

## サービスと対象範囲（未決定）

| 整理する項目 | 記入内容 |
| --- | --- |
| 題材・対象利用者 | 未決定：誰が、どの場面で使うか |
| 解決する課題 | 未決定：現状の困りごとと、利用後の変化 |
| 最小の利用シナリオ | 未決定：利用開始から目的達成までの一連の操作 |
| 初期機能（MVP） | 未決定：最小のシナリオに不可欠な機能 |
| 対象外 | 未決定：初期リリースで実装しない機能 |
| データ | 未決定：保存する項目、機密性、所有者、保存期間、削除方法 |
| 利用者と権限 | 未決定：匿名利用の可否、認証の必要性、利用者間のデータ分離 |
| 公開範囲 | 未決定：一般公開 / 閲覧者を限定したデモ等 |

提案：まず一つの主要シナリオに絞り、架空データで実演できる規模を検討します。認証、管理画面、通知などは題材上の必要性を確認してから対象に含めます。

## 機能要件の記録様式（提案）

機能決定後、次の項目を機能ごとに埋めます。

| 項目 | 記入内容 |
| --- | --- |
| 要件 ID・名称 | 一意な ID と利用者視点の名称 |
| 利用者・目的 | 誰が何を達成するための機能か |
| 前提条件 | 認証状態、権限、データの状態 |
| 入力・出力 | 必須項目、型、上限、画面・API の結果 |
| 正常系 | 操作後に期待する表示・保存状態 |
| 異常系・境界値 | 不正入力、存在しないデータ、重複、権限不足など該当する条件 |
| 受け入れ条件 | 「前提・操作・期待結果」で再現可能に記述 |
| 検証との対応 | テスト種別、テストケース、関連するセキュリティ要件 ID、結果の保存先 |

## 非機能要件の整理

| 項目 | 状態 | 決めること・検証の方向性 |
| --- | --- | --- |
| セキュリティ | 共通方針は決定済み、詳細は提案 | security.md の要件と検証を機能・構成に対応付ける |
| 再現性 | Docker 使用は決定済み、手順は未決定 | クリーンな環境から起動・マイグレーション・テストできる手順を整える |
| 性能 | 未決定 | 想定ユーザー数、データ量、応答時間目標と測定条件を決める |
| 可用性・復旧 | 未決定 | 稼働時間、停止許容、バックアップ、復旧目標と復元確認の方法を決める |
| 運用 | 未決定 | ログ、監視、通知、更新・ロールバック方法、保存期間を決める |
| 費用 | 未決定 | 月額上限、公開期間、予算通知と停止・撤去の手順を決める |
| UI・操作性 | 未決定 | 対応端末・ブラウザー、主要操作のキーボード対応などの受け入れ条件を決める |

## 技術選定の観点（最終選定は未決定）

| 対象 | 候補 | 比較する観点 |
| --- | --- | --- |
| フロントエンド | React を中心とする構成 / Next.js | レンダリング・SEO の必要性、ルーティング、配信方法、バックエンドとの責務分担、学習・運用負荷 |
| バックエンド | Laravel / FastAPI | PHP / Python の習熟度、認証など必要な機能、DB 操作・マイグレーション、テスト、保守性 |
| 自動テスト | PHP 採用時の PHPUnit 等（未選定） | 採用言語との整合、単体・API・DB 統合・主要シナリオの検証範囲 |

選定時には「必要な要件 → 候補の比較 → 採用理由 → 制約・見送った理由」を記録します。実測していない性能や費用を選定根拠として断定しません。

AWS ECS、Terraform、GitHub Actions、PostgreSQL、Docker の使用は決定済みです。一方、ECS の起動方式、DB の配置、入口・TLS・ドメイン、ネットワーク、リージョン、常時稼働の有無は未決定です。月額見積もりでは計算資源だけでなく、DB、入口、通信、ログ、イメージ・状態ファイルの保存なども含めます。

## 開発と受け入れの進め方（提案）

1. 題材・データ・初期機能を決め、受け入れ条件と簡単な脅威分析を作成する。
2. 予算・公開方法を踏まえて技術を選定し、データモデル・API・権限・構成を小さく設計する。
3. Docker での開発手順と CI の検査を整え、一つの利用シナリオずつ実装・検証する。
4. 公開前にセキュリティ、デプロイ・復旧手順、費用見積もりを確認する。

機能の完了条件案：受け入れ条件に対応したテストを通し、関連する検査結果・変更理由・残る制約を説明できること。必要な検査が失敗または未実施なら、検証済みとして扱いません。具体的なリリース条件は公開前に決定します。

## 意思決定と検証の記録（提案）

意思決定には日付、対象要件、候補、採用理由、不採用理由、制約、見直す条件を残します。検証には対象コミット、環境・ツールバージョン、手順、期待結果、実際の結果、証跡、未実施の理由を残します。保存場所は未決定です。

面接では、要件を一つ選び「なぜ必要か」「なぜその設計か」「どう確かめたか」「何が未解決か」を一続きで説明できる状態を目指します。
