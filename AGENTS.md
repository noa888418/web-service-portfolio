# 開発作業のルール

このファイルはリポジトリ全体の作業に適用します。既存の変更と文書の意図を尊重してください。

## 目的と説明

- 転職用の小規模 Web サービスとして、実装だけでなく設計理由・検証方法を本人が説明できる成果物を作ります。
- 作業説明、ドキュメント、変更報告は日本語を基本とします。識別子やツール名は必要に応じて英語を使用します。
- 決定済み・提案・未決定・未実施を区別します。未決定事項を暗黙に仕様や採用技術として扱わないでください。
- 作業前に README.md と関連する docs 内の文書を確認し、今回の作業範囲を守ってください。

## 技術と作業単位

- 題材は社内IT依頼・改善管理サービスです。React + TypeScript + Vite、Laravel、PostgreSQL、Docker、AWS ECS、Terraform、GitHub Actions の使用は決定済みです。AWS 月額予算は 3,000 円、公開は月 60 時間程度の事前案内期間のみ、独自ドメインは未所有です。
- AWS の基本構成・認証・撤去案は docs/architecture.md、見積もり条件は docs/costs.md を参照してください。ユーザー決定と設計上の提案・未検証事項を区別し、使用時間には構築・検証・撤去も含めて見積もってください。各製品のバージョンや追加の検査ツールは未選定です。
- 公開デモでも `APP_ENV=production`、`APP_DEBUG=false` の安全な実行設定を用いる設計案です。デモ投入の可否を APP_ENV だけで判断せず、用途・対象 DB・明示許可を確認する条件を守ってください。通常デプロイで `migrate:fresh` を使用しないでください。
- 変更は一つの目的で説明・レビュー・検証できる小さな単位に分けます。無関係な整理や機能追加を混ぜないでください。
- 実装前に目的、受け入れ条件、影響範囲、セキュリティ上の懸念、検証方法を整理します。
- 技術選定や設計変更では、候補、採用理由、不採用理由、制約を関連文書に残します。実装と文書の整合を保ってください。

## セキュリティと検証

- シフトレフトを採用し、要件・設計からセキュリティを考慮します。詳細は docs/security.md を参照してください。
- コンテナには必要最小限のベースイメージを使用し、非 root ユーザーで実行し、マルチステージビルドを採用します。
- 秘密情報を Git、コンテナイメージ、ログ、検証報告に含めないでください。実際の認証情報をテスト用データとして使用しないでください。
- 秘密情報検出、依存関係検査、イメージ検査、SBOM 生成を開発工程に組み込みます。ツール未導入を検査成功と記載しないでください。
- 秘密情報検査には `config/gitleaks.json` で固定した Gitleaks を使用します。手順は docs/development.md を参照し、コミット前にフックを登録してください。手動検査は `python scripts/secrets.py files` と `python scripts/secrets.py staged` で行い、秘密値は出力しないでください。
- 秘密情報検査の変更では `python -I scripts/test_secret_controls.py` で隔離検証します。本物の認証情報や完成した検証用トークンを本リポジトリに保存・コミットしないでください。
- Terraform の実行権限、ネットワーク境界、状態ファイルと plan の保護を設計・検証の対象にします。
- 変更に応じたテストと検査を実施します。文書のみの変更はリンク・内容の整合性などを確認し、未実装のアプリのテスト成功を主張しないでください。
- テスト・検査の削除や無効化、失敗の握りつぶし、合格するまでの基準緩和で成功扱いにしません。必須チェックの失敗や未実行を、`continue-on-error` や `|| true` 等で隠さないでください。
- 実行できない検証は「未実施」とし、理由・影響・実施に必要な条件を報告します。検査結果を捏造しないでください。

## 作業終了時の報告

次の内容を簡潔に報告してください。

1. 変更内容と変更理由（対象ファイル、要件との関係）。
2. 検証方法と結果（実行したコマンドや確認内容、成功・失敗・未実施の区別）。
3. 残る課題、未決定事項、必要な次の作業。

秘密情報を除いた検証証跡を残し、失敗や制約も説明可能な状態にしてください。
